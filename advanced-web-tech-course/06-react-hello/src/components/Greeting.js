// Greeting.js — the simplest possible component: it just returns some JSX.

function Greeting() {
  // This is the "Hello World" the practical asks for.
  return <h1>Hello, World!</h1>;
}

// `export default` lets other files import this component.
export default Greeting;
