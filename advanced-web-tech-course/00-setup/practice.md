# Module 0 — Practice

1. Print your Node.js and npm versions. Paste the output.
2. In one sentence, explain the difference between **Node.js** and **npm**.
3. Choose your MongoDB path (Atlas or local) and write down your connection
   string with the password removed. Why must the password never be committed
   to GitHub?
4. Create and run `hello.js` that prints your name and the current Node version.
5. **Short answer:** Why can JavaScript run outside the browser today, when it
   originally only ran inside web pages?
